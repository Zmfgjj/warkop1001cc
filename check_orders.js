const mysql = require('/var/www/backend/node_modules/mysql2/promise'); async function run() { const conn = await mysql.createConnection({ host: '127.0.0.1', user: 'root', password: '', database: 'warkop1001cc' }); try { const [rows] = await conn.query('SELECT id, nama_pelanggan, status, created_at FROM pesanan WHERE nama_pelanggan LIKE \'%rehan%\' OR nama_pelanggan LIKE \'%nabil%\' ORDER BY created_at DESC LIMIT 5'); console.log(rows); } catch(e) { console.error('Error:', e.message); } conn.end(); } run().catch(console.error);
