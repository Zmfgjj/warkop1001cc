const mysql=require('mysql2/promise');(async ()=>{const db=await mysql.createConnection({host:'127.0.0.1',user:'root',password:'',database:'warkop'});const [r]=await db.query('SELECT nama, kategori_id, kategori2_id, kategori_print_destination, kategori2_print_destination FROM menu WHERE nama LIKE '%roti%' OR nama LIKE '%indomie%' OR nama LIKE '%susu%'');console.log(r);process.exit(0)})();
